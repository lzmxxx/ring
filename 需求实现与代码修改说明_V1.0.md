# 需求实现与代码修改说明 V1.0

## 1. 交付结论

本次修改在完整阅读原固件、上位机和需求规格后，按“状态机统一、模块单一职责、事件驱动、资源有上限”的原则完成。原有采集和血氧算法主链得到保留，新增功能没有堆入一个大循环或交叉访问私有变量。

软件侧已完成：长按开关机、订阅门控、三种输出模式、连续/周期调度、RTC、运动状态、片内 Flash 记录、断点同步、统一控制协议、发送优先级及上位机控制与导出。最终 RAM 为 54.09%，相较修改前约 70% 明显下降，并为后续扩展保留 67,704 B。

本说明中的“已实现”表示代码、构建或自动测试已经验证；功耗、射频、RTC 长期精度、传感器效果和掉电恢复等必须依赖硬件的条目列为“待真机验收”，没有用软件测试冒充产品验收。

## 2. 需求实现状态

| 需求域 | 状态 | 实现摘要 |
|---|---|---|
| 长按开/关机 | 已实现 | PA0 持续 2 s；关机前停止业务、刷新记录元数据，进入 Standby |
| PA6 指示灯 | 已实现 | 开机确认点亮，长按关机确认立即熄灭 |
| BLE 订阅后采集 | 已实现 | 连接但未订阅不启动实时采集；记录模式可脱离 BLE 独立运行 |
| 三种输出模式 | 已实现 | Raw Only、Algorithm Only、Raw + Algorithm |
| 连续/周期模式 | 已实现 | 1 s 表示连续；15 s、30 s和任意 `>=15 s`；周期窗口 10 s |
| RTC 同步 | 已实现 | 上位机连接订阅后自动同步 Unix 时间和时区；备份域保存有效标记 |
| 绝对时间调度 | 已实现 | 下次起点基于前一计划起点推进，不把算法耗时累加进周期 |
| 运动检测 | 已实现 | SC7A20 INT/锁存状态，阈值和持续时间可配置，结果和状态均携带运动位 |
| 200 KiB Flash | 已实现 | 2 个元数据页 + 196 KiB 数据，16 B/条，CRC，启动扫描恢复 |
| 记录启停/擦除 | 已实现 | 异步低优先级处理，擦除状态事件，记录强制 15 s |
| Flash 满处理 | 已实现 | 停止新增记录、保留已有记录并上报 `APP_ERR_FLASH_FULL` |
| 历史同步/续传 | 已实现 | 指定记录序号开始，分片传输，断开后以上位机已收序号续传 |
| BLE 优先级 | 已实现 | 控制 > Raw > 实时结果 > 历史记录 |
| 上位机界面 | 已实现 | 模式/周期、记录、同步、状态、历史 CSV、Raw 双通道波形 |
| 体温 | 接口预留 | 实时包和 Flash 均有字段，V1 没有体温器件，当前固定 0 |
| 医疗血氧标定 | 未完成 | 继续明确标记“工程未标定”，需针对最终光机结构和人群标定 |
| 整机功耗/长期稳定性 | 待真机验收 | 需要电流仪、真实 BLE 链路、反复掉电和 8/16 h 实验 |

## 3. 固件架构修改

### 3.1 新增模块

- `User/app_device.c/.h`：设备配置、状态、错误、RTC 与绝对时间采集调度的唯一管理者。
- `User/app_protocol.c/.h`：统一应用帧、命令号和 CRC16-CCITT。
- `User/app_record.c/.h`：Flash 布局、擦除、追加记录、元数据、恢复和历史同步。

### 3.2 调整现有模块

- `app_main.c`：只编排模块初始化，不再在广播前常开传感器。
- `app_ppg.c/.h`：增加按需启停、上电稳定、三模式门控、周期窗口、Raw 聚合、FIFO 错误上报；Ring 元素只保存波形。
- `app_spo2.c`：按模式丢弃不需要的作业，结果增加 RTC、运动、质量和体温预留，并同时投递实时与记录队列。
- `app_ble.c/.h`：接收控制命令；连接/订阅状态进入设备状态机；拆分控制、Raw、结果、历史队列；删除失效发送分支。
- `app_packet.c/.h`：更新 A5 01、增加 A5 04；电量包保留字节清零，不夹带厂商调试镜像。
- `app_power.c`、`mydrivers/Power/bsp_power.c/.h`：传感器和 LED 电源分开控制，关机前刷新记录。
- `mydrivers/SC7A20/SC7A20.c/.h`：增加反初始化和运动参数运行期配置。
- `BLE_Driver/hl_test.c`：RTC 初始化不再每次复位备份域。
- `interrupt.c`：RTC 唤醒只通知状态任务，不在 ISR 内执行业务。

### 3.3 任务优先级和职责

| 线程 | 优先级 | 职责 |
|---|---:|---|
| power | 3 | 按键与关机序列 |
| ble | 4 | 协议栈、控制和 Notify |
| ppg | 6 | FIFO/DMA、Tag 配对、Ring 写入 |
| state | 8 | RTC 时间点与采集状态 |
| spo2 | 10 | 算法计算 |
| record | 12 | 擦除、写记录、历史读取 |

RT-Thread 数值越小优先级越高。Flash 历史同步最低，避免破坏实时 FIFO 和 BLE 控制。

## 4. RAM 与 Flash 优化

### 4.1 RAM

| 项目 | 修改 | 约节省 |
|---|---|---:|
| FFT 缓冲 | 红/红外顺序计算，复用 `fft_work + fft_output`，由 3 个 2048-float 数组降为 2 个 | 8 KiB |
| RT heap | 24 KiB 降为 8 KiB；业务任务与 IPC 均为静态对象 | 16 KiB |
| PPG Ring | 删除每条重复序号，仅保存 `PPG_Frame` | 3 KiB |
| 调试镜像 | 删除三组 VOFA Raw 全局镜像和电量包调试字段依赖 | 少量 |

最终 Keil 结果：

```text
Code=102776  RO-data=133520  RW-data=1868  ZI-data=77884
RAM: 77.88 KiB / 144 KiB = 54.09%，剩余 67,704 B
```

保留的 FIFO、DMA、BLE 和窗口复制计数器用于低成本现场定位，不保存整段调试波形。它们与已删除的高占用镜像变量性质不同。

### 4.2 Flash 分区

Keil IROM1 从 512 KiB 收缩为 312 KiB，防止链接器覆盖记录区：

| 地址 | 大小 | 用途 |
|---|---:|---|
| `0x08000000~0x0804DFFF` | 312 KiB | 固件 |
| `0x0804E000~0x0804E7FF` | 2 KiB | Metadata A |
| `0x0804E800~0x0804EFFF` | 2 KiB | Metadata B |
| `0x0804F000~0x0807FFFF` | 196 KiB | 记录数据 |

最终固件占应用区 230.76 KiB，剩余 83,192 B。记录容量为 `196 KiB / 16 B = 12,544` 条；15 s/条约可记录 52.27 小时。需求中的 200 KiB 是整个记录分区，扣除冗余元数据后实际数据为 196 KiB。

## 5. Flash 记录格式

```c
typedef struct __packed {
    uint32_t timestamp;   // Unix秒
    uint16_t sequence;    // 记录序号
    uint8_t  spo2;        // %
    uint8_t  heart_rate;  // BPM
    uint8_t  motion;      // 0静止/1运动
    uint8_t  quality;     // 0..100
    int16_t  temperature; // 0.01°C，V1为0
    uint8_t  flags;       // valid/calibrated
    uint8_t  reserved;
    uint16_t crc16;
} app_record_t;           // 16 B
```

启动记录会擦除旧分区，因此上位机先弹出确认。Flash 擦除在记录线程逐页执行并在页间让出 CPU；擦除前暂停采集。普通记录阶段不临时擦页，只编程一条已经擦除的 16 B 记录。

## 6. BLE 协议

### 6.1 控制帧

```text
AA 55 | Ver(1) | Cmd(1) | Seq(u16) | Len(u16) | Payload(0..10) | CRC16(u16)
```

CRC 为 CCITT，多字节字段小端。命令号：

| 命令 | 值 | 说明 |
|---|---:|---|
| TIME_SYNC | 0x01 | Unix 时间 + 时区分钟 |
| SET_OUTPUT_MODE | 0x10 | 1 Raw、2 Algorithm、3 两者 |
| SET_SAMPLE_PERIOD | 0x11 | 1 或 `>=15 s` |
| START/STOP_RECORD | 0x20/0x21 | 设备记录 |
| GET_RECORD_INFO | 0x22 | 三片返回 24 B 信息 |
| SYNC_RECORD | 0x23 | 从 uint16 记录序号开始 |
| ERASE_RECORD | 0x24 | 擦除记录 |
| GET_DEVICE_STATUS | 0x30 | 两片返回状态、版本和时间 |
| SET_MOTION_PARAMETER | 0x40 | 阈值、持续时间 |
| ACK/ERROR | 0x80/0x81 | 应答、异步事件、错误 |

### 6.2 实时数据

- `A5 01`：质量、flags、序号、RTC、SpO2、HR、PI、体温预留。
- `A5 02`：电量、电压、最近结果序号；其余保留字节为 0。
- `A5 04`：两个 Raw 双波长样本、第一样本序号、采样率、RTC。

Raw 原始 ADC 为 20 bit，而默认 ATT 有效数据为 20 B。当前选择每包两个样本，并将每个样本算术右移 4 bit 后以 `int16` 传输；上位机左移恢复量级。这会舍弃低 4 bit，是带宽与批量效率之间的明确取舍，不是无意截断。

## 7. 上位机修改

- `protocol.py`：新结果/Raw 解析、控制帧构造、CRC、分片状态机、设备状态、记录信息和历史记录 CRC。
- `ble_client.py`：连接后自动 RTC 同步和状态查询；提供通用命令发送及新事件信号。
- `ui/main_window.py`：工作模式、周期、设备记录、擦除确认、断点同步、状态和历史 CSV。
- `ui/chart_view.py`：新增 Raw 红光/红外曲线。
- `ui/dashboard.py`：主卡片显示 RTC、质量、体温预留和运动。
- `data_recorder.py`：实时 CSV 增加设备时间、质量和温度。
- `test_suite.py`：更新结果包并增加应用 CRC、分片状态测试。

## 8. 验证记录

### 8.1 固件

本地 `MDK-ARM/build_final_v1_retry.log`（构建日志按 `.gitignore` 不上传）：ARM Compiler 5 构建、链接和 BIN 导出成功，0 Error、0 Warning。一次全量并行重建出现 Keil 同名 `clock.c` 对象并行写入竞态 `L6449E`；在全量编译对象后再次执行正常构建即成功。全量编译中的 8 个告警均来自原厂 `vt2102_hal.c` 枚举类型混用，不属于本次业务代码。

### 8.2 上位机

- `python -m unittest -v test_suite.py`：9/9 通过。
- `py_compile`：入口、BLE、协议、记录和三个界面模块通过。
- Qt offscreen 冒烟测试：主窗口成功创建，模式 3 项、周期 4 项。

### 8.3 算法

- `verify_algorithms.py`：20/20 通过。
- `verify_firmware_algorithm.py`：时域 HR/R/SpO2、FFT HR/R/SpO2、配对数均与 Python 参考实现一致。

## 9. 必须继续做的真机验收

以下项目不能仅靠 PC 编译证明，烧录后应按需求文档第一、第二阶段验收逐项记录：

1. PA0 长按边界、PA6 时序、Standby 唤醒和关机电流。
2. 未连接、已连接空闲、连续、15 s、30 s、记录、历史同步各状态平均/峰值电流。
3. 15/30 s 连续数小时的采集起点误差和 RTC 漂移。
4. IPA1322 FIFO 在 BLE 拥塞、算法计算和 Flash 操作并发时是否溢出。
5. 擦除、写入和停止记录各阶段断电，再上电检查记录恢复与 CRC。
6. BLE 中途断开、重连、从已收序号续传，核对无重复/遗漏。
7. 8 h、16 h真实佩戴记录和 Flash 容量边界。
8. 最终结构下的 SpO2/HR 标定与运动伪影评价。

## 10. 后续维护约束

- 不要把 IROM1 扩大到 `0x4E000` 以上。
- 不要在中断中执行算法、BLE 发送或 Flash 写入。
- 不要用 `delay(15000)` 实现周期，也不要从算法结束时重新计周期。
- 不要绕过 `app_device` 直接在多个模块各存一份模式/周期状态。
- 不要改变 16 B Flash 格式而不提升格式版本。
- 新协议字段必须同时修改固件构造、上位机解析、自动测试和本文档。
- 增大静态缓冲区或任务栈前后都要保存 map/size 对比，并完成最长路径真机压力测试。
